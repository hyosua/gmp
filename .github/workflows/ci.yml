name: Continuous Integration & Security

on:
  push:
    branches: [ main, feat/** ]
  pull_request:
    branches: [ main ]

jobs:
  # 1. Job de scan de secrets (Gitleaks en mode local)
  secret-scanning:
    name: Secret Scanning
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks Scan
        uses: gitleaks/gitleaks-action@v2
        with:
          # On désactive l'upload des résultats pour éviter les erreurs de permission
          # si GitHub Advanced Security n'est pas activé.
          report-format: json
          fail: true

  # 2. Job de SAST (Léger avec njsscan)
  sast:
    name: SAST (njsscan)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: nodejsscan scan
        id: njsscan
        uses: ajinabraham/njsscan-action@v0.0.3
        with:
          args: '.'

  # 3. Job de Build, Lint & SCA
  build-and-test:
    name: Build, Lint & SCA
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: SCA (Software Composition Analysis)
        run: npm audit --audit-level=high

      - name: Check Lint
        run: npm run lint

      - name: Build project
        run: npm run build
